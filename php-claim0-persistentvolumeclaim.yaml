apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: php-claim0
  name: php-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
